apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: cic-api-backend-config
  namespace: cic-prod
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /health
    port: 8080
